import { TruncateAfterDashPipe } from './truncate-after-dash.pipe';

describe('TruncateAfterDashPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateAfterDashPipe();
    expect(pipe).toBeTruthy();
  });
});
